{
	"name": "shark-king",
	"version": "1.0.0",
	"private": true,
	"description": "Shark King — Cybersecurity portfolio (HTML/CSS/JS + Firebase)",
	"scripts": {
		"serve": "npx serve -s public -l 4173",
		"dev": "npx http-server public -p 4173 -c-1",
		"firebase:deploy": "firebase deploy --only hosting,firestore:rules",
		"firebase:emulators": "firebase emulators:start --only hosting,firestore"
	}
}